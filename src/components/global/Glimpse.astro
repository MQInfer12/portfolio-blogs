---
interface Props {
  color: string
  top: string
  left: string
}

const { color, top, left }: Props = Astro.props;
---

<div class="glimpse"></div>

<style lang="scss" define:vars={{ color, top, left }}>
  @import 'src/styles/variables.scss';

  .glimpse {
    position: absolute;
    width: 55rem;
    height: 55rem;
    background: var(--color);
    filter: blur(300px);
    z-index: -1;
    top: var(--top);
    left: var(--left);
  }
</style>